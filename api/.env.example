# Server
PORT=3001

# PostgreSQL (local via Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/machine_test_scheduler?schema=public"

# CORS — define a origem permitida para o frontend em produção.
# Deixe em branco para permitir todas as origens (não recomendado em produção).
# Exemplo: ALLOWED_ORIGIN=https://machine-test-scheduler.vercel.app
ALLOWED_ORIGIN=
