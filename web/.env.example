# Modo de dados:
#   demo — usa localStorage (sem backend, ideal para Vercel)
#   api  — usa o backend Express + PostgreSQL
VITE_DATA_MODE=demo

# URL da API (usado apenas quando VITE_DATA_MODE=api)
VITE_API_URL=http://localhost:3001/api
